server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Debjit-896/microservice-config
          default-label: main
          clone-on-start: true